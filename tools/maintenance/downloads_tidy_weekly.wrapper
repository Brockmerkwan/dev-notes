#!/bin/zsh
set -euo pipefail
setopt NULL_GLOB
LOCK="/tmp/downloads_tidy_weekly.lock"
if ! mkdir "$LOCK" 2>/dev/null; then exit 0; fi
trap 'rmdir "$LOCK" 2>/dev/null || true' EXIT
exec /usr/local/bin/downloads_tidy_weekly.real "$@"
